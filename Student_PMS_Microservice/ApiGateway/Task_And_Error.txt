ApiGateway
Task And Error Notes
Updated on 2026-03-13

Current Scope
- ApiGateway uses Ocelot and SwaggerForOcelot to route requests to:
  - UserService
  - ProjectService
  - ProjectGroupService

Priority 1 - Completed Fixes
1. Completed:
   JWT bearer authentication is configured in ApiGateway.
   Status:
   - Bearer auth registration exists in Program.cs
   - Ocelot protected routes use AuthenticationProviderKey = Bearer

2. Completed:
   SwaggerForOcelot bearer mapping was added.
   Status:
   - AddAuthenticationProviderKeyMapping("Bearer", "Bearer") is present in Program.cs
   - Gateway Swagger security definition is present

3. Completed:
   Duplicate auth-route overlap was removed from the gateway contract.
   Status:
   - Login and register are exposed as /api/Auth/login and /api/Auth/register
   - Protected service traffic remains under /api/UserService, /api/ProjectService, /api/ProjectGroupService

4. Completed:
   The hardcoded Kestrel HTTPS listener was removed.
   Why:
   - It caused startup failure when no explicit dev certificate was available.

Priority 2 - Current Known Risk
1. Gateway startup must still be verified on the local machine after restart.
   Current situation:
   - Source code builds
   - The main observed blocker was stale output/running process behavior and local HTTPS certificate sensitivity
   Impact:
   - If the gateway still does not launch locally, the next thing to check is local dev-certificate state and stale running processes

2. Gateway authentication must be verified through real requests after restart.
   Current situation:
   - Source-side bearer mapping is now present
   - Runtime verification is still needed for actual forwarded authenticated requests

Priority 3 - Remaining Tasks
1. Confirm one authenticated gateway request works for:
   - UserService
   - ProjectService
   - ProjectGroupService

2. If HTTPS launch still fails locally, repair local dev-certificate state and rebuild cleanly.

3. Add a small gateway smoke test set if you want repeatable route/auth verification.
