Student PMS Microservice
Global Tasks And Errors
Updated on 2026-03-16

Purpose
- This file tracks cross-project status, shared integration work, and remaining system-level risks.
- Project-specific details are tracked in each project's Task_And_Error.txt file.

Priority 1 - System State Right Now
1. Current code status:
   Core service code is in a much better state than the initial review.
   UserService, ProjectService, ProjectGroupService, ApiGateway, Comman, and Demo_TestProject all received direct fixes.

2. Current auth status:
   JWT token creation is implemented in UserService and the main service controllers are protected with [Authorize].
   Login and register are kept anonymous through AuthController.

3. Current cross-service status:
   ProjectService and ProjectGroupService call UserService to resolve CreatedByID, ModifiedByID, and ApprovedByID display names.
   ProjectGroupService also calls ProjectService to validate ProjectID.

Priority 2 - Completed Global Work
1. Completed:
   Shared API exception types were centralized in Comman.
   Status: Implemented in Comman/Exceptions/ApiException.cs and reused in services.

2. Completed:
   Placeholder cross-service client files and other confirmed-unused code were removed.
   Removed items:
   - UserService/Services/UserService/UserMicroservice.cs
   - ProjectService/Services/ProjectServices/ProjectMicroservices.cs
   - ProjectGroupService/Services/ProjectGroupServices/ProjectMicroservices.cs
   - Comman/Functions/OptionMapper.cs
   - ProjectGroupService/Services/ProjectGroupByProject/Validation/InsertProjectGroupByProjectValidation.cs

3. Completed:
   Individual services now expose Swagger bearer configuration where needed.
   Status:
   - UserService: present
   - ProjectService: added
   - ProjectGroupService: added

4. Completed:
   ApiGateway source now includes SwaggerForOcelot bearer mapping and gateway-side bearer security definitions.
   Status: Implemented in ApiGateway/Program.cs.

5. Completed on 2026-03-16:
   ApiGateway Development JWT settings were aligned with UserService JWT settings.
   Fix:
   - ApiGateway/appsettings.Development.json secret was updated to match UserService.
   Result:
   - Gateway token validation issue (401 due to mismatched secret in Development) is resolved.

6. Completed on 2026-03-16:
   ApiGateway authenticated routing with bearer token was runtime-verified by the user.
   Status: Confirmed working.

Priority 3 - Current Global Risks
1. ApiGateway runtime/startup is still environment-sensitive.
   Current code fix:
   - The hardcoded Kestrel HTTPS listener was removed from ApiGateway/Program.cs.
   Current remaining risk:
   - Local launch can still fail if stale output is running or if the local HTTPS developer certificate is missing/outdated.
   Impact:
   - Gateway startup on a machine must be verified after clean stop/rebuild/restart.

2. RabbitMQ/eventing remains unfinished.
   Affected area:
   - UserService/RabbitMQ
   Impact:
   - Messaging is not a reliable current system capability.

3. Demo integration tests remain environment-dependent.
   Impact:
   - Cross-service behavior is still not protected by a controlled automated integration test setup.

Priority 4 - Current Suggested Order
1. Restart and verify ApiGateway locally after clearing stale output/debug sessions.
2. Decide whether RabbitMQ is required; either implement fully or remove the dead/commented code.
3. Replace live-environment integration tests with controlled integration infrastructure.
