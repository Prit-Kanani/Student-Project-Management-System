ProjectService
Task And Error Notes
Updated on 2026-03-11

Priority 1 - Completed Runtime Fixes
1. Fixed on 2026-03-11:
   Program.cs now registers:
   - IProjectServices
   - IProjectRepository
   - InsertValidation
   - UpdateValidation
   Status: Resolved in code.

2. Fixed on 2026-03-11:
   ProjectRepository.GetProjectView now awaits FirstOrDefaultAsync correctly.
   Status: Resolved in code.

3. Fixed on 2026-03-11:
   ProjectController update endpoint now uses PUT and explicit [FromBody] binding.
   Status: Resolved in code.

Priority 2 - Completed Cross-Service Integration
1. Fixed on 2026-03-11:
   ProjectService now calls UserService to resolve CreatedByID and ModifiedByID names for project view responses.
   Status: Implemented in code through Services/External/UserServiceClient.cs.

2. Fixed on 2026-03-11:
   ProjectService now exposes a ProjectID existence endpoint for downstream validation.
   Status: Implemented in code through ProjectController and service/repository contracts.

3. Fixed on 2026-03-11:
   The unused ProjectMicroservices placeholder file was removed.
   Status: Resolved in code.

4. Fixed on 2026-03-11:
   Shared API exception handling now uses Comman exceptions instead of service-local ApiException copies.
   Status: Resolved in code.

Priority 3 - Remaining Work
1. DeactivateProject uses Delete in the controller but performs a soft delete by setting IsActive = false.
   Impact: Behavior is valid, but should be documented consistently as a soft delete.

2. Startup configuration is still minimal compared with UserService.
   Impact: Validation and middleware patterns are not fully standardized across services.

Priority 4 - Environment Note
1. Fresh build verification after the latest changes is blocked in this environment because NuGet restore cannot reach api.nuget.org.

Suggested Next Steps
1. Re-run `dotnet build ProjectService/ProjectService.csproj` in a network-enabled environment.
2. Document the delete endpoint clearly as a soft delete.
3. Standardize startup/middleware/validation patterns if you want closer parity with UserService.
