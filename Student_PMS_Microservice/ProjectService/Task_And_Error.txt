ProjectService
Task And Error Notes
Updated on 2026-03-13

Priority 1 - Completed Runtime Fixes
1. Completed:
   Program.cs now registers the required services and validators.
   Status:
   - IProjectRepository
   - IProjectServices
   - InsertValidation
   - UpdateValidation

2. Completed:
   ProjectRepository.GetProjectView async read logic was fixed.

3. Completed:
   ProjectController update endpoint uses PUT and explicit body binding.

4. Completed:
   JWT authentication is configured and ProjectController is protected with [Authorize].

5. Completed:
   Swagger bearer configuration is now present.

Priority 2 - Completed Cross-Service Work
1. Completed:
   ProjectService resolves CreatedByID and ModifiedByID names by calling UserService.

2. Completed:
   ProjectService exposes a ProjectID existence endpoint for downstream validation.

3. Completed:
   Shared API exception handling now uses Comman exceptions.

4. Completed:
   Unused placeholder microservice file was removed.

Priority 3 - Current Remaining Work
1. Delete endpoint behavior is a soft delete.
   Current behavior:
   - controller route says Delete
   - data path sets IsActive = false
   Impact:
   - The contract should stay documented clearly as a soft delete

2. Startup/middleware patterns are simpler than UserService.
   Impact:
   - This is not broken, but service setup is not fully standardized across projects

Priority 4 - Next Checks
1. Verify one authenticated ProjectService call directly.
2. Verify the same call through ApiGateway once gateway runtime verification is complete.
