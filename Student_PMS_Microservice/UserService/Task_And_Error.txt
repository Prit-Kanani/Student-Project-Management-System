UserService
Task And Error Notes
Updated on 2026-03-13

Priority 1 - Completed Auth Fixes
1. Completed:
   Login flow was corrected.
   Status:
   - AuthController login uses AuthService
   - token generation is working
   - password hash comparison logic is confirmed valid

2. Completed:
   Register endpoint was added beside login in AuthController.
   Status:
   - POST /api/Auth/login
   - POST /api/Auth/register

3. Completed:
   JWT startup validation is in place.
   Status:
   - Jwt settings are validated during startup/service use

4. Completed:
   Protected controllers now use [Authorize].
   Status:
   - UserController
   - RoleController
   - UserProfileController
   AuthController remains anonymous for login/register.

Priority 2 - Completed Service Work
1. Completed:
   Shared exception handling now uses Comman exceptions.

2. Completed:
   UserProfile slice is no longer empty.
   Status:
   - controller, service, repository, and DTOs are implemented

3. Completed:
   Audit-name resolution endpoint exists for downstream services.
   Status:
   - UserController exposes ResolveAuditUsers

4. Completed:
   Old leftover HomeController is no longer present.

Priority 3 - Current Known Risks
1. JWT behavior depends on matching configuration across all validating services and gateway.
   Impact:
   - If any service uses a different secret/issuer/audience, auth will fail across services

2. RabbitMQ area is still unfinished and partially commented or inactive.
   Affected folder:
   - UserService/RabbitMQ

Priority 4 - Remaining Tasks
1. Decide whether RabbitMQ/event publishing is required.
   If yes:
   - implement and verify it fully
   If no:
   - remove the dead/commented code

2. Re-verify direct login/register plus one protected UserService endpoint after any further auth config changes.
