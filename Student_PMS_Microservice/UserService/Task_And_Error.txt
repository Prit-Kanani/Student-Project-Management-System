UserService
Task And Error Notes
Generated from repository review on 2026-03-11

Priority 1 - High-Risk Runtime Errors
1. Fixed on 2026-03-11:
   - AuthController.Login now awaits AuthService.Login.
   - JWT settings are validated during startup and auth service construction.
   Status: Resolved in code and UserService build is passing.

Priority 2 - Unfinished Features
1. Fixed on 2026-03-11:
   - UserProfileController now exposes endpoints.
   - UserProfileService now delegates to repository logic.
   - UserProfileRepository now performs database reads and writes.
   - UserProfile DTO classes now contain fields.
   Status: Basic UserProfile CRUD path is implemented.

2. RabbitMQ integration files are fully commented out.
   Impact: Event-driven integration is unfinished.

3. UserMicroservice is only a placeholder with no outbound integration logic.
   Impact: Intended service-to-service communication is not implemented.

Priority 3 - Logic And Consistency Issues
1. Fixed on 2026-03-11:
   - RoleController now applies validators for create and update.
   - Incorrect not-found logic in UsersService.UpdateUser and RoleService.UpdateRole was corrected.
   - UserRepository not-found paths were switched to custom API exceptions.
   Status: Resolved in code.

2. UserRepository.GetUserView may return null and then maps the result directly without explicit not-found handling.
   Impact: Null behavior depends on mapper behavior instead of explicit service logic.

3. Fixed on 2026-03-11:
   - AuthRepository no longer takes unused IConfiguration.
   Status: Resolved in code.

Priority 4 - Quality Issues
1. There are many nullable warnings across DTOs, models, services, repositories, and Program.cs.
2. UserService contains a HomeController with a basic Add action that does not appear related to the service domain.
   Impact: This may be leftover/demo code rather than production API behavior.

Priority 5 - Tasks
1. Remaining:
   Re-enable and complete RabbitMQ only if messaging is required.
2. Remaining:
   Implement or remove unused UserMicroservice placeholder logic.
3. Remaining:
   Reduce nullable warnings and review leftover/demo controllers such as HomeController.
4. Remaining:
   Review UserRepository.GetUserView and RoleRepository null-safety warnings for stricter handling.
